  <div class="card-player" data-toggle="modal" data-target="#exampleModalLong-<%= selection.id %>">
    <div class="card-player-content">
        <%= image_tag selection.player.team.icon %>
        <div class="card-player-title">
            <p><%= selection.player.first_name %><span> <%= selection.player.last_name %></span></p>
        </div>
        <div class="player-rating">
            8.4 <i class="fas fa-star"></i>
        </div>
    </div>
  </div>
<div class="modal fade" id="exampleModalLong-<%= selection.id %>" tabindex="-1" role="dialog" aria-labelledby="exampleModalLongTitle" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLongTitle"><%= selection.player.position %> </h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <%= simple_form_for(:request, url: players_path, method: :get, remote: true)   do |f| %>
        <div id="search">
          <div class="row justify-content-center align-items-center">
            <div class="col-12 d-flex justify-content-center">
              <div class="row w-50">
                <div class="col-9 px-2 my-auto">
                  <%= f.input :last_name, placeholder: "Type this little brat's name", label: false %>
                </div>
                <div class="col-3 d-flex px-0">
                  <%= f.button :submit, "Find your new Morty", class: "home-btn" %>
                </div>
              </div>
            </div>
          </div>
          <br>
          <div class="row justify-content-center align-items-center">
            <div class="col-3">
              <%= f.input :team, placeholder: "How much for him, you cheapfuck", collection: Team.all.map(&:name) %>
            </div>
          </div>
        </div>
      <% end %>

        <div class="card-player-grid" id="players-<%= selection.id %>">
          <% Player.where(position: selection.player.position).first(40).each do |player| %>
            <div class="card-player">
              <div class="card-player-content">
                <%= image_tag player.team.icon %>
                <div class="card-player-title">
                  <%= player.first_name %><span> <%= player.last_name %></span></p>
                </div>
                <div class="player-rating">
                 8.4 <i class="fas fa-star"></i>
                </div>
              </div>
            </div>
          <% end %>

        </div>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        <button type="button" class="btn btn-primary">Save changes</button>
      </div>
    </div>
  </div>
</div>

#partiaslier tout ce qu'il y dnas la div players selcetion.id
# passer players à cette partiale
#en JS retrouver le formulaire (query selector) , poser un écouteur sur le formulaire , 
#dès qu'on clique submit, retrouver les valeurs dans les inputs
#une fois input retrouvé, faire fetch sur player index, utislier le résultat dans la partiale de players id
