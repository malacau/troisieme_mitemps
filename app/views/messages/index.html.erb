
<% if @messages.empty?%>
<h3> Aucune discussion en cours. Soyez le premier à envoyer un message 💪 </h3>
<% else %>
<h2> Discussion <%= @ligue.name %></h2>
<% end %>

<div class="container-messages" id="messages" data-chatroom-id="<%= @ligue.id %>">
  <% @messages.each do |message| %>
    <%= render 'shared/card_message', message: message %>
  <% end %>
</div>

<%= simple_form_for [@ligue, @message] do |f| %>
  <%= f.input :content, as: :string, label: false, placeholder: "Balance ta meilleure punchline", input_html: { id: 'input_shadow'}%>
<% end %>


<%# <textarea class="form-control text required" id="input_shadow" placeholder="Balance ta meilleure punchline" name="message[content]"></textarea> %>